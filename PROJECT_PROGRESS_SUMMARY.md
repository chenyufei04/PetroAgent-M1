# PetroAgent 项目进展总结

> 总结日期：2026-08-12  
> 当前版本：PetroAgent v0.10.1 / Knowledge Foundation v0.3.0  
> 当前定位：面向聚合物驱科研实验的本地化、可追溯工程分析原型平台

## 1. 项目概况

PetroAgent 已经从最初的规则验证 Demo，发展为一套覆盖物理模拟、实验分析、工程规则、
知识图谱、方案推荐和本地大模型交互的完整原型。当前主线以聚合物驱参数敏感性实验为
示范领域，通过统一的实验与方案上下文，将 OPM Flow 计算结果、技术经济指标、规则执行、
推荐结论和 Neo4j 证据关系连接在同一条解释链中。

当前系统形成了以下闭环：

```text
OPM Flow 物理模拟
→ 水驱基准配对
→ 聚合物驱参数敏感性分析
→ 聚合物质量衡算与增量经济评价
→ 压力、设施和经济规则验证
→ 方案确定性排序与推荐
→ 指标、规则、推荐和证据语义绑定
→ Neo4j 知识图谱
→ FastAPI 服务
→ Vue 交互工作台
→ Ollama / Qwen3 8B 解释交互
```

## 2. 已完成的实验工作

### 2.1 聚合物驱参数实验

已完成 `polymer_sensitivity_v1` 参数敏感性实验：

- 3 个聚合物浓度；
- 3 个注入速率；
- 共 9 个参数组合；
- 9 个 OPM Flow 算例全部成功；
- 已保存算例级清单、模拟产物、汇总数据和完整时间序列；
- 已实现成功算例复用，支持中断后避免重复计算已有成功结果。

### 2.2 独立水驱基准

已建立 `waterflood_baseline_v1`，与聚合物驱实验目录和配置相互独立，不污染已有实验。
系统按相同注入速率进行水驱—聚合物驱配对，用于计算聚合物驱的真实增量效果。

### 2.3 实验分析产物

当前已经生成：

- 算例汇总 CSV；
- 动态时间序列 CSV；
- 水驱—聚合物驱配对终值和配对时间序列；
- 累计产油、含水率和压力敏感性结果；
- 参数响应矩阵、热力图和动态曲线；
- 水驱—聚合物驱对比图；
- 技术经济排名 CSV；
- Markdown 分析报告和排名报告；
- 机器可读的指标、规则执行、推荐和解释链 JSON/CSV。

## 3. 已实现的确定性工程计算

### 3.1 聚合物质量衡算

系统根据聚合物浓度、注入水速率和段塞时间窗口，对聚合物注入质量进行积分计算，并输出：

- 段塞持续时间；
- 段塞期累计注入水量；
- 聚合物总质量和吨数；
- 日配聚峰值；
- 单吨聚合物增量油。

### 3.2 增量经济评价

系统已实现简单增量经济模型，能够计算：

- 增量油收入；
- 聚合物成本；
- 增量注水成本；
- 增量产水处理成本；
- 总增量成本；
- 净增量价值；
- 盈亏平衡聚合物价格；
- 盈亏平衡油价；
- 经济正值状态。

当前经济参数标记为 `illustrative_unvalidated`，仅用于演示和方法验证，尚未经过现场标定，
不能直接视为投资决策结论。

### 3.3 工程约束

当前能够检查：

- 注入井井底压力上限；
- 注水设施能力；
- 产水处理能力；
- 日配聚能力；
- 聚合物供应总量；
- 聚合物质量衡算有效性；
- 水驱基准配对完整性；
- 净增量价值是否大于零。

规则执行记录已经包含实际值、阈值、单位、运算符、通过状态、消息、模型 ID 和证据来源。

## 4. 方案排序与推荐

9 个聚合物驱方案已形成确定性排名。排序优先考虑技术约束和经济正值，其次比较净增量
价值和增量油。系统能够明确区分：

- 技术约束通过；
- 经济评价通过；
- 当前排序中的相对候选；
- 已批准的现场实施方案。

当前排名第一仅表示在演示性评价口径下相对更优，不代表已经通过经济评价或现场审批。
系统能够比较任意候选方案，也能够解释第一名与第二名在净值、增量油、聚合物成本和
技术状态上的差异。

## 5. 知识图谱进展

Neo4j 已支持 Docker Compose 部署，同时保留直接连接本机或 Neo4j Desktop 的兼容方式。
图谱已经从静态领域知识扩展到实验实例和规则执行实例，当前包含：

- 领域概念、参数、指标和单位；
- 工程规则、阶段和证据来源；
- 实验、模拟算例、Deck、参数集和模拟运行；
- 数据集、报告和水驱—聚合物驱比较；
- 经济评价、指标观测和规则执行实例；
- 推荐结果及其规则和证据关系。

已形成三类主要解释链：

```text
SimulationCase → MetricObservation → Concept
SimulationCase → RuleExecution → Rule → Source
SimulationCase → Recommendation → RuleExecution → Evidence
```

Neo4j 离线时，FastAPI 可以回退到同源的本地解释 JSON，保证核心页面仍能读取实验结果。

## 6. FastAPI 服务进展

FastAPI 当前提供：

- 服务健康检查；
- 案例、实验和实验详情查询；
- 技术经济排名查询；
- 当前方案统一上下文；
- 单方案指标—规则—推荐—证据解释链；
- 受控实验文件下载；
- CSV/XLSX 文件上传和预览；
- 通用规则分析；
- Neo4j 预定义图谱查询；
- 实验方案排名和图谱查询；
- 智能体状态和对话接口。

所有 HTTP 调用均生成结构化 JSONL 日志，默认保存在 `outputs/logs/api_calls.log`。单个文件
达到 1000 条记录或 10 MiB 时自动轮转，默认保留 5 个历史文件。日志不会保存请求正文、
密码或认证头，并已由 Git 忽略。

## 7. Vue 工作台进展

页面已调整为助手优先的渐进式布局。首次进入时主要展示：

- PetroAgent 平台标题；
- 居中并占据主要空间的交互式工程分析助手；
- 页面右侧固定的模型和 Neo4j 运行状态；
- 底部折叠的数据导入、通用规则分析与完整知识库管理入口。

用户发出展示或分析指令后，页面按需展开：

- 当前实验和方案选择；
- 推荐结论与排名；
- 工程参数和指标；
- 规则执行与失败证据；
- 当前方案知识解释图谱；
- 实验图表和全部方案排名。

页面支持点击规则、指标、图谱节点和排名行查看详情；支持收起分析区；点击“清空对话”
后会清除聊天记录并恢复首次进入的简洁状态。

右侧状态面板当前提供一个可操作的模型下拉框。现阶段只有 `Qwen3 8B`，界面结构已为
未来多模型选择预留，但尚未实现真正的后端动态模型切换。

## 8. Ollama 与交互智能体

当前本地大模型链路为 Ollama + `qwen3:8b`，同时保留 Mock 模式用于无模型联调。
智能体已经实现：

- 当前实验和方案绑定；
- 最近 10 条消息的有限多轮上下文；
- 问题主题识别和主题切换隔离；
- 只检索当前问题相关的指标与规则；
- 重复回答检测和重写；
- 单位缺失保护和演示性结论边界；
- 模型连续回答不合格时使用结构化证据降级；
- 受控页面区域联动；
- 只读保护，禁止模型启动 Flow、修改规则或直接回写 Neo4j。

四个快捷入口已经使用确定性逻辑增强：

- “为什么推荐当前方案？”列出全部候选方案，点击任意方案后解释其推荐或不推荐原因；
- “只看未通过规则和证据”直接列出失败规则、实际值、阈值和单位；
- “展示关键指标和图谱”汇总当前方案关键指标并展开解释图谱；
- “查看全部方案排名”返回方案总数、当前名次和前三名摘要。

## 9. 测试与验证状态

当前已覆盖：

- OPM Deck 运行与路径处理；
- 参数扫描和结果复用；
- 水驱—聚合物驱配对；
- 聚合物质量和经济计算；
- 工程约束与排名；
- 语义解释链；
- Neo4j 导入和查询；
- FastAPI 实验与智能体接口；
- Ollama 提供器；
- 智能体主题聚焦、排名比较和快捷入口；
- API 日志写入与按条数轮转。

最近一次后端回归结果为 `54 passed`；Vue 生产构建通过。页面关键交互已经通过实际浏览器
检查，包括助手优先首屏、指令展开、清空恢复、方案选择按钮和右侧固定状态面板。

## 10. 当前工程边界

当前系统仍属于科研原型，不是可直接暴露到公网的生产平台。尚未完成：

- 用户登录、身份认证和角色权限；
- 多用户与多项目数据隔离；
- 公网 HTTPS、Host 限制和接口限流；
- 真正的多模型动态切换；
- 文献 PDF 向量索引和完整文档 RAG；
- 已训练并部署的 QLoRA 适配器；
- 在线 OPM 后台任务队列和计算资源调度；
- 审批式规则修改和 Neo4j 写回；
- 现场经济参数、设施能力和规则阈值标定；
- FastAPI、Vue、Neo4j、Ollama 和 OPM Flow 的完整生产容器编排；
- 自动备份、集中日志、监控和告警。

## 11. 建议的下一阶段

建议按以下顺序继续：

1. 固化 v0.10.1 当前成果，整理提交边界并建立可回滚版本；
2. 为智能体建立问题—证据—回答评测集，量化回答准确率和重复率；
3. 把模型下拉框接入后端模型注册表，实现真实多模型选择；
4. 建立文献和工程标准的可审计向量检索层；
5. 使用现场或可信项目数据标定经济参数、压力和设施约束；
6. 将 OPM Flow 长任务迁移到独立 Worker 和任务队列；
7. 增加登录、权限、限流和项目数据隔离；
8. 完成单机 Linux Docker Compose 公网只读演示版；
9. 在安全和审批机制完善后，再开放在线模拟和知识图谱写回。

## 12. 阶段结论

PetroAgent 当前已经能够完整演示“物理模拟—实验数据—确定性计算—工程规则—方案排序—
知识证据—大模型解释—页面联动”的科研分析闭环。其主要价值已经不只是运行一个聚合物
驱 Demo，而是验证了一种可迁移的石油工程智能分析架构：确定性计算负责事实，规则和知识
图谱负责验证与溯源，大模型负责受控解释和交互。

下一阶段的重点不应继续堆叠页面功能，而应转向模型评测、现场标定、任务调度、权限安全
和生产部署，使当前原型逐步具备真实工程应用条件。
