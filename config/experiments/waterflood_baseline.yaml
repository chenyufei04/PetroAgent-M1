# v0.9.2 独立水驱基准。先运行 scripts/prepare_waterflood_baseline.py 生成基础 Deck。
experiment_id: waterflood_baseline_v1
analysis_case_id: polymer_simple2d
data_nature: OPM数值模拟数据
comparison_role: baseline
paired_experiment_id: polymer_sensitivity_v1
base_deck: data/processed/waterflood_baseline/2D_THREEPHASE_WATER_HETER.DATA
output_root: outputs/experiments
design: cartesian
max_cases: 3

flow_args: []

parameters:
  injection_rate:
    token: "{{PETRO_PARAM_INJECTION_RATE}}"
    values: [100, 150, 200]
    unit: "m3/day"
    file_glob: "**/*"
    format: ".6g"
